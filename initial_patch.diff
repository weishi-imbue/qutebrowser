diff --git a/qutebrowser/browser/shared.py b/qutebrowser/browser/shared.py
index 384a69c30..4185a7226 100644
--- a/qutebrowser/browser/shared.py
+++ b/qutebrowser/browser/shared.py
@@ -159,23 +159,43 @@ _JS_LOGMAP_MESSAGE: Mapping[usertypes.JsLogLevel, Callable[[str], None]] = {
 }
 
 
-def javascript_log_message(
+def _js_log_to_ui(
     level: usertypes.JsLogLevel,
     source: str,
     line: int,
     msg: str,
-) -> None:
-    """Display a JavaScript log message."""
-    logstring = f"[{source}:{line}] {msg}"
+) -> bool:
+    """Check if a JavaScript message should be shown in the UI, and show it if so.
 
-    for pattern, levels in config.cache['content.javascript.log_message'].items():
+    Returns True if the message was shown in the UI, False otherwise.
+    """
+    levels_config = config.cache['content.javascript.log_message.levels']
+    for pattern, levels in levels_config.items():
         if level.name in levels and fnmatch.fnmatchcase(source, pattern):
+            # Check excludes
+            excludes_config = config.cache['content.javascript.log_message.excludes']
+            for exc_pattern, msg_patterns in excludes_config.items():
+                if fnmatch.fnmatchcase(source, exc_pattern):
+                    if utils.match_globs(msg_patterns, msg) is not None:
+                        return False
             func = _JS_LOGMAP_MESSAGE[level]
-            func(f"JS: {logstring}")
-            return
+            func(f"JS: [{source}:{line}] {msg}")
+            return True
+    return False
+
+
+def javascript_log_message(
+    level: usertypes.JsLogLevel,
+    source: str,
+    line: int,
+    msg: str,
+) -> None:
+    """Display a JavaScript log message."""
+    if _js_log_to_ui(level, source, line, msg):
+        return
 
     logger = _JS_LOGMAP[config.cache['content.javascript.log'][level.name]]
-    logger(logstring)
+    logger(f"[{source}:{line}] {msg}")
 
 
 def ignore_certificate_error(
diff --git a/qutebrowser/config/configdata.yml b/qutebrowser/config/configdata.yml
index 220712e2d..93a8d6160 100644
--- a/qutebrowser/config/configdata.yml
+++ b/qutebrowser/config/configdata.yml
@@ -941,27 +941,52 @@ content.javascript.log:
     `error`.
 
 content.javascript.log_message:
+  renamed: content.javascript.log_message.levels
+
+content.javascript.log_message.excludes:
+  type:
+    name: Dict
+    keytype: String
+    valtype:
+      name: List
+      none_ok: true
+      valtype: String
+    none_ok: true
+  default: {}
+  desc: >-
+    Glob-based exclusions to suppress specific JavaScript messages from being
+    shown in the qutebrowser UI.
+
+    The keys are glob patterns matching the source of JavaScript messages.
+    The values are lists of glob patterns matching the message text.
+    If a message's source matches a key and its text matches any of the
+    corresponding message patterns, it will not be shown in the UI even if
+    it would otherwise be shown by content.javascript.log_message.levels.
+
+content.javascript.log_message.levels:
   type:
     name: Dict
     keytype: String
     valtype:
       name: FlagList
       valid_values:
+        - debug: Show JS debug as messages.
         - info: Show JS info as messages.
         - warning: Show JS warnings as messages.
         - error: Show JS errors as messages.
+    none_ok: true
   default:
     "qute:*": ["error"]
     "userscript:*": ["error"]
   desc: >-
-    Javascript message sources/levels to show in the qutebrowser UI.
-
-    When a JavaScript message is logged from a location matching the glob
-    pattern given in the key, and is from one of the levels listed as value,
-    it's surfaced as a message in the qutebrowser UI.
+    Defines which JavaScript log message levels from matching sources are
+    shown in the qutebrowser UI.
 
-    By default, errors happening in qutebrowser internally or in userscripts are
-    shown to the user.
+    The keys are glob patterns matching the source of JavaScript messages.
+    The values are lists of log levels to show for matching sources.
+    When a JavaScript message is logged from a source matching the glob pattern
+    and is from one of the listed levels, it's surfaced as a message in the
+    qutebrowser UI.
 
 content.javascript.modal_dialog:
   type: Bool
